diff --git a/Dockerfile b/Dockerfile
index 3637818..090b105 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,17 +1,21 @@
-FROM jrottenberg/ffmpeg:3.1-alpine AS ffmpeg
 
 FROM tensorflow/tensorflow:0.12.0-gpu
-COPY --from=ffmpeg / /
 EXPOSE 7000
 WORKDIR /src
 ENV FLASK_APP /src/server/server.py
 ENV DEBIAN_FRONTEND noninteractive
 ENV PYCURL_SSL_LIBRARY=openssl
 COPY . .
-RUN apt-get install -y libcurl
+RUN add-apt-repository ppa:mc3man/trusty-media
+RUN apt-get update
+RUN apt-get install -y ffmpeg \
+&& apt-get install -y libssl-dev \
+&& apt-get install -y libcurl4-openssl-dev
+
 RUN curl -Lo /usr/local/bin/youtube-dl https://yt-dl.org/downloads/latest/youtube-dl
 RUN chmod a+rx /usr/local/bin/youtube-dl
 RUN youtube-dl --version
 RUN pip install -r requirements.txt
 RUN chmod +x stylize.sh
-RUN flask run --host 'localhost' --port '7000'
+#RUN flask run --host 'localhost' --port '7000'
+
